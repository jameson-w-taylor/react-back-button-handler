import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-back-button-handler',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
